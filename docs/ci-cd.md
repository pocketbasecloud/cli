<!--
  Pointer, not a copy. These guides are maintained on the docs site, at
  langding_page_v4/src/content/docs/ci-cd/ in the monorepo. This file stays
  because the README, the portal, and people's bookmarks have linked to it —
  do not restore the prose here, or the two will drift.
-->

# Deploy from GitHub Actions

This guide lives on the PocketBase Cloud docs site:

### → **[pocketbasecloud.com/docs/ci-cd/deploying-from-github-actions](https://pocketbasecloud.com/docs/ci-cd/deploying-from-github-actions)**

It is the five-minute path: deploy once from your computer, copy your token
from the portal's **Account** page, save it as a GitHub secret named
`PB_TOKEN`, and add one workflow file.

For environments, monorepos, PR previews, exit codes, and every error message
with its fix, see the
**[CI/CD reference](https://pocketbasecloud.com/docs/ci-cd/reference)**.
